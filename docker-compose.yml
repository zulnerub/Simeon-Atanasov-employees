services:
  backend:
    build:
      context: ./backend
    container_name: employees-backend
    ports:
      - "8080:8080"

  frontend:
    build:
      context: ./frontend
    container_name: employees-frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
